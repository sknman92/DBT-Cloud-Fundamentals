macros:
  - name: cents_to_dollars
    description: a macro that converts dollars to cents and rounds to the user defined decimal places, or 2 if none is provided
    arguments:
      - name: column_name
        type: string
        description: the name of the column you want to convert
      - name: decimals
        type: integer
        description: number of decimal places, defaults to 2