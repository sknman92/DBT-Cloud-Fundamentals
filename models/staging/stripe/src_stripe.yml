version: 2

sources:
  - name: STRIPE
    database: TIL_PORTFOLIO_PROJECTS
    schema: DBT_CY_STRIPE
    tables:
      - name: PAYMENT
        loaded_at_field: _batched_at
        freshness:
          warn_after:
            count: 12
            period: hour
        
